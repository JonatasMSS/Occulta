[project]
name = "projeto"
version = "0.1.0"
description = "Selective face anonymization pipeline"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "deepface",
    "numpy",
    "opencv-python",
    "retina-face",
    "tqdm",
]
